// Declaration file for leaflet.heat (no @types package available)
declare module 'leaflet.heat' {
    // leaflet.heat is a side-effect plugin — it mutates the L namespace.
    // No exports needed; importing it extends L.heatLayer automatically.
}